spring:
  h2:
    console:
      enabled: true  # Ativa o console H2 para visualizar o banco de dados via web
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb  # URL de conex�o para um banco de dados H2 em mem�ria
    username: sa
    password:
  hibernate:
    ddl-auto: update  # Modo de atualiza��o autom�tica das tabelas (outros valores poss�veis: create, create-drop, validate, none)
  jpa:
    hibernate:
      show-sql: true  # Exibe as instru��es SQL no console

server:
  port: 8080  # Porta em que o aplicativo Spring Boot ser� executado
